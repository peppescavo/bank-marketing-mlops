apiVersion: v1
kind: Service

metadata:
  name: bank-marketing-api

spec:
  selector:
    app: bank-marketing-api

  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080

  type: NodePort